{% extends 'admin/layout/layout_main.html.twig' %}
{% block title_detailed %}{% endblock %}
{% block body %}
    <div class="row">
        <div class="col-xl-12">
            <div class="card">
                <div class="card-body">
                    <div class="text-end ">
                        <a href="{{ path('admin_pricing_plans_show') }}" class="btn btn-primary ">{% trans %}back.label{% endtrans %}</a>
                    </div>
                    {{ include('admin/pages/pricing/_form.html.twig') }}
                </div>
                <!-- end card body -->
            </div>
            <!-- end card -->
        </div>
    </div>
{% endblock %}

